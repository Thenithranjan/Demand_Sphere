# ============================================
# Retail AI - FastAPI Backend Dependencies
# ============================================

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database — Supabase PostgreSQL via SQLAlchemy
sqlalchemy==2.0.35
psycopg2-binary            # PostgreSQL adapter (replaces pymysql) — uses prebuilt wheel
pg8000>=1.31.0


# Supabase Client — Storage API + Auth helpers
supabase==2.9.1

# Environment Variables
python-dotenv==1.0.1

# Data Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Authentication
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
cryptography==43.0.1          # Still needed by python-jose

# CORS & Middleware
python-multipart==0.0.12
